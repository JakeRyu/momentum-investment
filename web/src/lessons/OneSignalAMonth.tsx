/**
 * Lesson 4. Prose carried from the phase 3 home page, where it sat as
 * the "One Signal a Month" section.
 */
export default function OneSignalAMonth() {
  return (
    <>
      <p>
        Whichever of the six suits you, they all run on the same clock.
        Every strategy here decides once a month. The signal is computed at
        the month-end close, and the allocation it produces holds through
        the whole month that follows — it doesn&rsquo;t rebalance because a
        headline broke or a market moved on a Tuesday.
      </p>
      <p>
        That holds even if you&rsquo;re late to act. The allocation to move
        into is still the one set at the last month-end, never a fresher
        read on today&rsquo;s prices — acting mid-month applies a
        different, untested rule, and the next scheduled rebalance is
        already on its way regardless. Remembering that one date, every
        month, without fail, is the part a phone does better than a person.
      </p>
      <p>
        It also means the work is small. Once a month you check what the
        rule says, compare it to what you hold, and place the trades that
        differ — often none at all. The rest of the month there is nothing
        to do, and nothing you should do.
      </p>
    </>
  )
}
